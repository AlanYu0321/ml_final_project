[project]
name = "policy-ops-agent"
version = "0.1.0"
description = "Capstone policy/compliance assistant with RAG, tools, memory, governance, and evals."
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

